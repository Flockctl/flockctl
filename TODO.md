- [ ] Adopt `.claude/commands/*` during existing-directory import — introduce a project-level commands concept (like skills/agents), detect in scan preview, register in DB, reconcile via symlinks. For now imports leave the folder untouched.

- [ ] **Missions list page (`/missions`) + `GET /missions` API.** The sidebar previously linked to `/missions`, but neither the route nor the page existed — clicking the link rendered the React Router default 404 ("Unexpected Application Error"). The link is now hidden in [`ui/src/components/shell/BrowseNav.tsx`](ui/src/components/shell/BrowseNav.tsx) (commented placeholder between `Tasks` and `Schedules`) and the sidebar order test in [`ui/src/__tests__/components/shell/sidebar.test.tsx`](ui/src/__tests__/components/shell/sidebar.test.tsx) was relaxed to 7 rows. Restore both when the list page lands. What needs to happen:
  - **Backend:** add `GET /missions` to [`src/routes/missions.ts`](src/routes/missions.ts) with standard `lib/pagination` paging, plus query filters `?status=`, `?autonomy=`, `?project_id=`. Index-friendly — `missions` already has indexes on `(project_id, status)`. Schema: returns `{ items, page, limit, total }` with the same row shape as `GET /missions/:id`.
  - **Frontend hooks:** `useMissions(query)` in [`ui/src/lib/hooks/missions.ts`](ui/src/lib/hooks/missions.ts) mirroring the existing detail hook, with cache key `["missions", "list", normalizedQuery]`.
  - **Page:** `ui/src/pages/missions.tsx` — pattern after [`ui/src/pages/incidents.tsx`](ui/src/pages/incidents.tsx) (SectionHeader + table component). Columns: objective (linked to `/missions/:id`), project, status pill, autonomy pill, budget remaining (tokens / USD), updated_at. Empty state: "No missions yet — open a project's Plan tab to start one" with a project picker shortcut.
  - **Router + breadcrumb:** register `{ path: "missions", element: <MissionsPage />, handle: { breadcrumb: missionsHandle } }` in [`ui/src/main.tsx`](ui/src/main.tsx) above the existing `/missions/:missionId` row. Add `missionsHandle` to [`ui/src/lib/route-handles.ts`](ui/src/lib/route-handles.ts) — the breadcrumb test at [`ui/src/__tests__/components/shell/breadcrumb.test.tsx:338`](ui/src/__tests__/components/shell/breadcrumb.test.tsx) already expects `/missions → Missions`.
  - **Sidebar restoration:** un-comment the `{ to: "/missions", … }` row in [`ui/src/components/shell/BrowseNav.tsx`](ui/src/components/shell/BrowseNav.tsx) and revert the test in [`ui/src/__tests__/components/shell/sidebar.test.tsx`](ui/src/__tests__/components/shell/sidebar.test.tsx) to 8 rows including "Missions" between Tasks and Schedules.
  - **Tests:** route test for `GET /missions` (pagination, filters, empty result, unknown filter rejected at zod), unit test for the page (empty state vs. populated, row click navigation, filter UX), update the existing breadcrumb test if the handle shape changes. E2E: extend `ui/e2e/missions-ui.spec.ts` with a list-page navigation case.
  - **Out of scope:** mission deletion from the list (already a destructive action, keep it inside the detail page), bulk operations, saved filter presets.

- [ ] **Mission creation flow — wire the dashboard "Start a mission" CTA end-to-end.** The button currently exists in [`ui/src/pages/dashboard-components/ActiveMissionCard.tsx`](ui/src/pages/dashboard-components/ActiveMissionCard.tsx) (empty-state branch) but the dashboard mounts the card with `mission={null}` and **no** `onStartMission` handler ([`ui/src/pages/dashboard.tsx:215`](ui/src/pages/dashboard.tsx)), so clicks are silently no-op (the card's `onClick={() => onStartMission?.()}` is documented as "stays clickable but inert" when no handler is wired). The backend half exists — `POST /missions/` ([`src/routes/missions.ts:285`](src/routes/missions.ts)) creates a mission row from `{ projectId, objective, autonomy?, budgetTokens?, budgetUsdCents? }`. What's missing on the UI side:
  - **`useCreateMission` hook** in [`ui/src/lib/hooks/missions.ts`](ui/src/lib/hooks/missions.ts) — react-query `useMutation` posting to `/missions/`, invalidating `["missions", "project", projectId]` on success. Mirrors the read-side hooks already in that file.
  - **`CreateMissionDialog` component** under `ui/src/pages/dashboard-components/` (or a shared `mission-detail-components/` if reused from project-detail) — form fields: project picker (defaults to the dashboard's primary project), `objective` textarea (required, 5–500 chars to match the route's zod schema), autonomy radio (`manual` / `semi` / `full`, default `semi`), optional budget tokens + budget USD cents inputs. On submit → call the hook, navigate to `/missions/{newId}` so the operator lands on the live mission view.
  - **Wire the dashboard** to mount the dialog and pass `onStartMission={() => setDialogOpen(true)}` into `<ActiveMissionCard mission={null} onStartMission={…} />`. Remove the `mission={null}` literal once the dashboard has an "active mission" data source — separate concern, leave for later.
  - **Listing route** is also missing — `/missions` is not registered in [`ui/src/main.tsx`](ui/src/main.tsx); only `/missions/:id` exists. Either add a list page or skip it (the dashboard card + project tree panel are the two entry points; an explicit list may not be needed for v1).
  - **Tests:** unit test for the dialog (renders, validates objective length, calls the mutation), integration test for the dashboard wiring (button click opens dialog, submit navigates). E2E covered by an existing `dashboard.spec.ts` extension if we want a smoke.
  - **Out of scope:** mission editing UI (autonomy / budget patch), mission listing page, "switch active mission" UX. Those land in separate slices once the create path is shipped.

- [ ] **Project-level git actions: commit & push (follows the `git pull` button).** Once the pull endpoint lands, extend the same surface with:
  - **Commit:** `POST /projects/:id/git-commit` with `{ message, paths?: string[] }`. UI: a small dialog showing `git status --porcelain` checkboxes (stage selection), commit-message textarea, and the resulting `commit` SHA on success. Reject empty index, reject empty message, reject if working tree is detached HEAD.
  - **Push:** `POST /projects/:id/git-push` with `{ remote?: "origin", setUpstream?: boolean }`. Surface auth failures (`stderr` contains `Permission denied`, `could not read Username`, `403`) as a structured `reason: "auth_failed"` so the UI can show a "configure your git credentials" hint instead of a raw stack trace. Default to pushing the current branch only — never `--all`, never `--force`. A separate `force?: boolean` flag stays opt-in and ideally hidden behind a confirmation modal.
  - **Shared infrastructure:** factor a `runGitCommand(projectPath, args, opts)` helper used by pull/commit/push. Centralise: timeout, sanitised stderr, mapping of common git error patterns → structured `reason` codes, audit log entry per invocation.
  - **UI:** group pull/commit/push under a single "Git" dropdown button on the project header once there are 3+ actions, instead of 3 standalone buttons polluting the row.
  - **Out of scope (explicit):** branch switching, merge-conflict resolution UI, interactive rebase, credential management. For those, keep directing the user to a terminal — Flockctl is not trying to replace `git`.

- [ ] **Desktop app (Electron) — VSCode-style installable build.** Wrap the existing daemon + UI in a native window with installers for macOS and Linux (Windows is out per CLAUDE.md §6). Sketch:
  - **Scope (in):** new top-level `apps/desktop/` package — kept out of the published npm artefact (do NOT broaden the `files` allowlist in [package.json](package.json), see CLAUDE.md §5). The CLI npm package stays unchanged.
  - **Scope (out):** rewriting the daemon, replacing the web UI, native modules in Rust, Windows builds, in-app terminal/editor (Flockctl is an orchestrator, not an IDE).
  - **Process model:** Electron `main` process forks `dist/server-entry.js` via `child_process.fork` on a **random free port** (not the fixed `52077` — must coexist with a globally-installed CLI daemon). `BrowserWindow` loads `http://127.0.0.1:<port>` and injects the bearer token via `session.webRequest.onBeforeSendHeaders` so the existing auth path (`src/middleware/auth.ts`) is untouched. WebSocket `?token=` query param keeps working as-is.
  - **Lifecycle:** main process owns the daemon child — `app.on('before-quit')` sends `SIGTERM`, waits up to 5s, then `SIGKILL`. Crash of the daemon child surfaces as an in-window error overlay with a "Restart daemon" button, not a silent dead UI. Single-instance lock via `app.requestSingleInstanceLock()` — second launch focuses the existing window instead of spawning a second daemon.
  - **Native module rebuild:** `better-sqlite3` ships a prebuilt for Node ABI, not Electron ABI. Wire `@electron/rebuild` into the desktop package's `postinstall` and into the `electron-builder` build step. Verify on both `darwin-arm64` and `darwin-x64` (Rosetta users exist).
  - **Packaging:** `electron-builder` produces `.dmg` (mac universal: `arm64` + `x64`), `.AppImage` and `.deb` (Linux x64 + arm64). Config lives in `apps/desktop/electron-builder.yml`. App ID: `com.flockctl.desktop`. Bundle the daemon's `dist/`, `migrations/`, and `node_modules` (production-only) inside `app.asar.unpacked/` — `better-sqlite3` `.node` files MUST be unpacked or `dlopen` fails.
  - **Code signing & notarization:** Apple Developer ID Application cert + `@electron/notarize` on macOS (notarization is non-negotiable for Gatekeeper post-10.15). Linux: no signing, but `.deb` should set `Maintainer` and a real `Description`. Secrets (`APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_TEAM_ID`, `CSC_LINK`, `CSC_KEY_PASSWORD`) live in GitHub Actions secrets, never in the repo.
  - **Auto-update:** `electron-updater` pointed at GitHub Releases — same channel that already drives the npm publish workflow ([.github/workflows/publish.yml](.github/workflows/publish.yml), CLAUDE.md §7). Prerelease versions (`X.Y.Z-rc.N`) flow to a `beta` channel; stable to `latest`. Update check on app start + every 6h while running. **Never auto-restart silently** — show a "Restart to install update" toast.
  - **CI:** new `desktop-build` job in GitHub Actions, matrix `[macos-14, ubuntu-22.04]`, triggered on tag push (`vX.Y.Z`) and PRs touching `apps/desktop/**`. Tag builds upload `.dmg`/`.AppImage`/`.deb` as Release assets alongside the existing npm publish. PR builds upload as workflow artefacts only — no signing on PRs (avoids leaking certs to forks).
  - **Native UX hooks:** app menu (File / Edit / View / Window / Help), `Cmd+Q`/`Cmd+W` semantics, dock icon, deep-link handler for `flockctl://` (route `flockctl://project/<id>` → focus + navigate). Tray icon is **out of scope** for v1 — adds platform-specific permission prompts on Linux and confuses the "is the daemon running" mental model.
  - **Open questions:** (a) should the desktop app **also** expose the daemon on `52077` for external CLI/SDK use, or strictly keep it on the random port and require users to install the npm CLI separately? (b) auto-update for the bundled daemon when the user has a newer npm-installed `flockctl` — do we share `~/.flockctl/` or namespace the desktop app's data dir? (c) telemetry — none for v1, but decide before shipping where the line is.

## Nice to have — not currently planned

- [ ] **P2P file sync between Flockctl daemons.** Selective, manual sync of skills, MCP configs, and workspace/project configs between two daemons over the existing SSH-tunnel transport (see `docs/REMOTE-ACCESS.md`). Sketch:
  - **Scope:** `~/.flockctl/skills/`, `~/.flockctl/mcp/`, `~/.flockctl/workspaces/<id>/{config,skills,mcp}/`, `<project>/.flockctl/{config,skills,mcp}/`. Never: SQLite DB, logs, PIDs, `.claude/skills/` symlink farm, `*-state.json` (regenerated).
  - **Secrets stay local.** MCP configs sync as-is including `${secret:NAME}` placeholders; raw tokens rejected with `400 raw_secret_in_config`. Pulling a config that references an undefined secret leaves the MCP disabled with a `mcp.secret_missing` event.
  - **Manual triggers only** — no watchers, no polling. CLI: `flockctl sync diff|push|pull <remote> [<glob>…]` with `--dry-run`, `--profile`, `--all`. Profiles in `~/.flockctl/sync-profiles/`.
  - **API:** `GET /sync/manifest?scope=…`, `GET/PUT/DELETE /sync/file?scope=…&path=…` with `If-Match`. Conflicts return 409; displaced bytes go to `import-backup/<ts>/`. Workspace identity via `sync_workspace_mappings(local_id, remote_label, remote_id)`.
  - **UI is first-class** — Sync workbench page (filters + checkbox table + bulk push/pull), per-entity quick-sync dropdowns on skill/MCP/workspace pages, top-bar `↑3 ↓1 !1` badge, profile manager, remote-workspace mapping picker, secret-missing banners on MCP cards, sync-history view.
  - **Conflict resolution = agent task.** Add `last_synced_hash` baseline column so the agent gets a real three-way merge. Conflict spawns a Flockctl task with inputs in `.flockctl/sync-conflicts/<id>/{base,local,remote,PROMPT}.md`; agent writes `resolved.*` + `resolution-notes.md`; UI shows three-column review (local / resolved / remote) with Apply / Edit / Reject / Re-run. File-type policy in `~/.flockctl/sync-conflict-policy.json` (agent / agent-proposal / manual). Default = human approval required, no auto-apply.
  - **Open questions if revisited:** batching multiple conflicts into one agent run vs one-per-file; provider selection per merge; baseline backfill strategy on upgrade; whether `resolution-notes.md` should be persisted to `.flockctl/sync-history/`.
