import { describe, it } from "vitest";

// plan-generator module was removed — plan persistence moved to filesystem (plan-store.ts)
describe.skip("persistPlan (removed — now filesystem-based)", () => {
  it("was replaced by plan-store.ts", () => {});
});
