# repo with every conflict

AGENTS.md (unmanaged) + CLAUDE.md (differs) + .mcp.json. Used to cover
the `--yes` blanket-consent branch in `buildImportActions`: all three
unresolved cases collapse into actions in one call.
